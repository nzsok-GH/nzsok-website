import { Navigation } from "nzsok-website";

// Fixed top nav with the NZSOK logo + dropdown links. `transparent` switches
// the bar from the frosted ivory default to a flat ivory used over heroes.
export const Default = () => <Navigation />;

export const Transparent = () => <Navigation transparent />;
